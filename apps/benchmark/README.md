# Benchmark

Benchmark orchestration and thesis experiment entry points belong here.

Current benchmark implementation remains in `src/experiments/` and is exposed as
the benchmark application boundary.
