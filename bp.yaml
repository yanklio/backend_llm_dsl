root:
  name: test
  database:
    type: sqlite
    database: ./data/test.db
    synchronize: true
    logging: false
  features:
    cors: true
    swagger: true
modules:
  - name: Test
    generate: [controller, service, module, entity, dto]
    entity:
      fields:
        - name: name
          type: string
          required: true